name: Generate Bootstrap Archives

on:
  workflow_dispatch:

jobs:
  bootstrap:
    name: Generate Bootstrap (${{ matrix.arch }})
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/termux/package-builder:latest

    strategy:
      fail-fast: false
      matrix:
        arch:
          - aarch64
          - arm

    env:
      NDK_VERSION: "r29"
      NDK_DIR: "/github/home/lib/android-ndk-r29"
      REPO_URL: "https://yatonorai.github.io/terminal-packages/apt/termux-main"

    steps:
      - name: Checkout repository (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Android NDK
        shell: bash
        run: |
          mkdir -p /github/home/lib
          if [ ! -d "${NDK_DIR}" ]; then
            curl -fLo /tmp/ndk.zip \
              "https://dl.google.com/android/repository/android-ndk-${NDK_VERSION}-linux.zip"
            unzip -q /tmp/ndk.zip -d /github/home/lib/
            rm /tmp/ndk.zip
          fi

      - name: Create output directories
        shell: bash
        run: mkdir -p output/${{ matrix.arch }} output/repo

      - name: Download and fix Packages index
        shell: bash
        run: |
          ARCH="${{ matrix.arch }}"
          MIRROR="/tmp/mirror"
          mkdir -p "$MIRROR/dists/stable/main/binary-${ARCH}"
          mkdir -p "$MIRROR/dists/stable/main/binary-all"

          curl -fsSL "${REPO_URL}/dists/stable/Release" -o "$MIRROR/dists/stable/Release"
          curl -fsSL "${REPO_URL}/dists/stable/InRelease" -o "$MIRROR/dists/stable/InRelease"
          curl -fsSL "${REPO_URL}/dists/stable/main/binary-all/Packages" \
            -o "$MIRROR/dists/stable/main/binary-all/Packages"
          curl -fsSL "${REPO_URL}/dists/stable/main/binary-${ARCH}/Packages" \
            -o "$MIRROR/dists/stable/main/binary-${ARCH}/Packages"

          # Remove Recommends problemáticos usando sed
          sed -i '/^Recommends:.*command-not-found/d' \
            "$MIRROR/dists/stable/main/binary-${ARCH}/Packages"
          sed -i 's/Recommends: command-not-found, bash-completion//' \
            "$MIRROR/dists/stable/main/binary-${ARCH}/Packages"
          sed -i 's/Recommends: bash-completion, command-not-found//' \
            "$MIRROR/dists/stable/main/binary-${ARCH}/Packages"
          sed -i 's/, command-not-found//' \
            "$MIRROR/dists/stable/main/binary-${ARCH}/Packages"
          sed -i 's/command-not-found, //' \
            "$MIRROR/dists/stable/main/binary-${ARCH}/Packages"

          echo "=== Verificando bash no Packages ==="
          grep -A 8 "^Package: bash$" "$MIRROR/dists/stable/main/binary-${ARCH}/Packages"

      - name: Start mirror server
        shell: bash
        run: |
          REAL_REPO="${REPO_URL}"
          cat > /tmp/server.py << EOF
          import os, urllib.request, urllib.error
          from http.server import HTTPServer, BaseHTTPRequestHandler

          REAL = "${REAL_REPO}"
          MIRROR = "/tmp/mirror"

          class H(BaseHTTPRequestHandler):
              def log_message(self, f, *a): pass
              def do_GET(self):
                  local = MIRROR + self.path
                  if os.path.isfile(local):
                      data = open(local, "rb").read()
                  else:
                      try:
                          data = urllib.request.urlopen(REAL + self.path).read()
                      except:
                          self.send_response(404)
                          self.end_headers()
                          return
                  self.send_response(200)
                  self.send_header("Content-Length", len(data))
                  self.end_headers()
                  self.wfile.write(data)

          HTTPServer(("0.0.0.0", 8080), H).serve_forever()
          EOF
          python3 /tmp/server.py &
          sleep 2
          echo "Servidor iniciado!"
          curl -s "http://localhost:8080/dists/stable/main/binary-${{ matrix.arch }}/Packages" \
            | grep -A 5 "^Package: bash$"

      - name: Generate bootstrap archive (debug)
        shell: bash
        run: bash generate-bootstrap-archive.sh -r "http://localhost:8080"

      - name: Generate bootstrap archive (release)
        shell: bash
        run: bash generate-bootstrap-archive.sh -g -r "http://localhost:8080"

      - name: Upload bootstrap archives
        uses: actions/upload-artifact@v4
        with:
          name: bootstrap-${{ matrix.arch }}
          path: |
            output/bootstrap-*-${{ matrix.arch }}.zip
            output/bootstrap-*-${{ matrix.arch }}.zip.9
          retention-days: 30
